import QtQuick
import "../"
// import '../Libs'

Rectangle {
    radius: Config.globalRadius
    color: Qt.alpha(Config.colors.accent,0.5)
    border.color: Config.colors.accent
    border.width: 2
}
